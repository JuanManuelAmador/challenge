The framework used was Python/Selenium, in order to run it first open the terminal and go to the path that the project was unzipped, then run:

pip install -r requirements.txt

this will install the dependencies, to run the test cases run:

python main.py
